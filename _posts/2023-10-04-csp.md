## csp
```
content security policy
we can set it in meta tag in the response
we can specify resources can be loaded from where in this part
for images,for scripts,for videos
it block usually inline js
but if we want want to use it we can specify nonce to the script tage and specify the nonce in meta tag

```
